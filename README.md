# Использование классов и объектов на основе популярной темы e-commerce.

## Описание:

## Установка:

+ Установите на свой компьютер GitHub.
+ Клонируйте репозиторий с GitHub  с помощью веб-URL.
```
 
```


## Использование:

В текущем виде, программа запускается в модуле main.py:



## Примеры использования функций:

+ Модуль category

**Создан класс Category** 

Класс для представления категории продукта
Имеет следующие свойства следующие свойства:

название (name),
описание (description),
список товаров категории (products).

    
+ Модуль product

**Создан класс Product** 

Класс для представления категории продукта
Имеет следующие свойства следующие свойства:

название (name),
описание (description),
цена (price),
количество в наличии (quantity).


+ Модуль utils

**def read_json(JSON_FILE) -> dict**
Функция читает JSON файл.


**def create_objects_from_json(data)**
Функция создает объекты классов.

 

+ Модуль main
Проверка выводов category.py и product.py
 

## Разработка:

+ находится в стадии разработки. 


## Тестирование:
  

Тесты созданы в пакете tests с использованием библиотеки pytest.
В тестах используются фикстуры. 
  
**Запуск тестов:**
```bash
pytest

```

**Запустить тесты с выводом покрытия в терминал**
```bash
pytest --cov=src --cov-report=term-missing

```


